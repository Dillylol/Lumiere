# App development

See the [repository README](../README.md) for setup and the [release progress](../docs/release-progress.md) for implementation status.

Run `npm ci`, then `npm run dev` for the web app or `npm run tauri dev` for desktop.

Validation: `npm test`, `npm run test:scripts`, `npm run build`, and `npm run test:e2e`.
